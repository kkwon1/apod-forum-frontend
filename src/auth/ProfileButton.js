import React from "react";

const ProfileButton = () => {
  const profileStyle = {
    cursor: "pointer",
    "margin-right": "20px",
  };
  return <div style={profileStyle}>Profile</div>;
};

export default ProfileButton;
